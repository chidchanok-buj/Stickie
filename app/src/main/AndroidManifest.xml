<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wireless.stickie" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".ShuffleOSI" >
        </activity>
        <activity android:name=".tcpLayersLearning" />
        <activity android:name=".CongestionControlActivity" />
        <activity android:name=".RoutingProtocolsActivity" />
        <activity android:name=".arqsActivity" />
        <activity android:name=".ModelLayerActivity" />
        <activity android:name=".MainTopic" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>